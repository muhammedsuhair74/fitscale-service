// export const publishEventController = async (event: any) => {
//   const event = await Event.create({
//     name: event.name,
//     data: event.data,
//   });
// };

// export const createEventController = async (event: any) => {
//   const events = await Event.findAll();
//   return events;
// };
